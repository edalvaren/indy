
export const speed = (number) => {
    return number + " RPM"
};

export const current = (number) => {
    return number + " A"

};

export const frequency = (number) => {
    return number + " Hz"
};
